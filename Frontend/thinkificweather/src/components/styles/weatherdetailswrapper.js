import styled from 'styled-components'

const WeatherDetailsWrapper = styled.div`
flex-basis: 100%;
display: flex;
flex-wrap: wrap;
padding: 10px 0;
margin: 20px 0;
background-color: rgba(0, 0, 0, 0.2);
border-radius: 10px;
align-self: flex-start;
`;

export default WeatherDetailsWrapper;